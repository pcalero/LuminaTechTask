import { Node } from './node';

/**
 * Subtree model
 * 
 * @export
 * @class Subtree
 * @author pedro.calero
 */
export class Subtree {
  arguson: {
    children: Array<Node>;
  }

  /**
   * Creates an instance of Subtree.
   * @memberof Subtree
   */
  constructor (){
  }

}